# PostgreSQL connection used by the FastAPI backend
DATABASE_URL=postgresql+psycopg://portfolio_user:portfolio_password@localhost:5432/portfolio_analytics

# The React development server
FRONTEND_URL=http://localhost:5173

# Add a real key only inside .env, never in this example file
MARKET_DATA_API_KEY=replace_with_your_api_key